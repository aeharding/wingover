# Seeded from the existing team distribution cert via `match import`
# (no new certificate minted) — encrypted with its own passphrase.
git_url("https://github.com/aeharding/wingover-match")

storage_mode("git")

type("appstore")
