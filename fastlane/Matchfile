# Shared per-team match repo (fastlane's recommended layout): the team
# distribution cert lives here once; wingover only adds its own
# provisioning profile alongside Voyager's.
git_url("https://github.com/aeharding/voyager-match")

storage_mode("git")

type("appstore")
